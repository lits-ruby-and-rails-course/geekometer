<div class="row">
	<div class="col-md-4">
		<%=if current_user.avatar.url.nil?
		   then
       			image_tag(current_user.avatar.default_url)
           else
       			image_tag(current_user.avatar.url)
     	   end %>
	</div>
	<div class="col-md-6"><font size="5">Current geekness:	</font>
	</div>
	<div class="col-md-2"><%= link_to "Edit profile", edit_user_path(current_user), class: "btn btn-default"%>
	</div>
	<hr style="height:50pt"/>
	<div align="center">
		<%= link_to "Create new question", new_question_path, class: "btn btn-default btn-lg"%>
	</div>
</div>

<p><h2><%=current_user.username%></h2></p>
