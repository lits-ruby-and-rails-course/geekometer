<% if user_signed_in? %>
    <p style="font-size: 25px; ">
      <%= "Editing User Profile"%>
    </p>
    <div class="col-md-8">
        <%= form_for current_user, :html => {:multipart => true} do |f| %>
            <p>
              <%= image_tag(current_user.avatar_url) if current_user.avatar? %>
            <p>
              <%= "Medium Avatar" %>
              <%= image_tag(current_user.avatar.url(:medium)) if current_user.avatar? %>
              <%= "Small Avatar" %>
              <%= image_tag(current_user.avatar.url(:small)) if current_user.avatar? %>
            </p>
            </p>
            <p>
              <%= "Upload image" %>
              <%= f.file_field :avatar %>
              <%= f.hidden_field :avatar_cache %>
              <%= "User Name"%> <%=current_user.username %>
              <%= f.text_field (:username) %>
            </p>
            <div class="actions">
              <%= f.submit %>
            </div>
        <% end %>

    <div class="skills">
      <% Topic.all.each do |topic| %>
          <label><input type="checkbox" value="<%= topic.id%>"><%= topic.name%></label>
      <% end %>
    </div>
    </div>
<% end %>

<div class="col-md-1">
<%= link_to "Back to Profile", user_path(current_user), class: "btn btn-default"%>
</div>